name: Deploy Application
description: Deploy application with environment-specific strategy
inputs:
  environment:
    description: Target environment for deployment
    required: true
runs:
  using: composite
  steps:
    - name: Intent - Deploy application
      run: echo "INTENT: Deploy application"
      shell: bash
    - name: Target environment
      run: echo "Target environment: ${{ inputs.environment }}"
      shell: bash
    - name: Deployment strategy info
      run: echo "Deployment strategy varies per environment"
      shell: bash
